
        <nav class=" navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container">
                <!--1-->
                <div class="nav-item">
                  <a class="navbar-brand" asp-controller="Home" asp-action="Index">SoftSpace</a> 
                </div>
                <div class="navbar-collapse collapse d-sm-inline-flex flex-sm-row-reverse">
                    <ul class="navbar-nav flex-grow-1">
                        <!--2-->
                        <li class="nav-item">
                             <a class="nav-link" asp-area="" asp-controller="User" asp-action="ShowCategories">@ViewBag.Bn_names[9]</a> 
                        </li>
                      @{
                          bool is_user  = false;
                      }
                        
                      @foreach (int role in @ViewBag.User.types_user )
                      {
                            
                        
                          @if(role == -1)
                          {
                            <li class="nav-item">
                              
                               <a class="nav-link" asp-area="" asp-controller="Home" asp-action="Authorization">@ViewBag.Bn_names[0]</a> 
                            </li>
                          }
                         
                          @if(role == 1)
                          {
                            is_user = true;
                            <li class="nav-item">
                              <a class="nav-link" asp-area="" asp-controller="Home" asp-action="LogOut">@ViewBag.Bn_names[5]</a> 
                            </li>
                            <li class="nav-item">
                              
                               <a   asp-controller="User" asp-action="Profile" class="nav-link">@ViewBag.Bn_names[17]</a>
                            </li>
                          }
                          @if(role == 2)
                          {
                            <li class="nav-item">
                              <a   asp-controller="Admin" asp-action="Index" class="nav-link">@ViewBag.Bn_names[6]</a>
                            </li>
                         
                          }
                        

                          @if((@ViewBag.User.id_dev>0)&&(role == 1))
                          {
                            <li class="nav-item" >
                              <a   asp-controller="Dev" asp-action="Index" class="nav-link">@ViewBag.Bn_names[7]</a>
                            </li>
                          
                          }
                      
                     
                          
                      }
                    </ul>       
                </div>
                      @if( is_user)
                          {
                            <div>
                               <ul class="navbar-nav flex-grow-1">
                                 <li class="nav-item">
                                  <a   asp-controller="User" asp-action="Cart" class="nav-link">@ViewBag.Bn_names[18] @ViewBag.User.quantity_cart_places  </a>
                                 </li>
                               </ul>
                            </div >
                            
                          }   

                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                
                

                
            </div>
            
        
        
        <div   class="form-inline form_search">
          

          <form method="get" asp-controller="User" asp-action="SearchProduct"  class="form_line">
            <ul class="navbar-nav flex-grow-1"> 
            <li class="nav-item">     
            <input class="form-control mr-sm-2 search_line" type="search"
             name="name_product" placeholder="@ViewBag.Bn_names[2]" aria-label="Search" value="@ViewBag.Search_product_name"> 
            </li>
           <li class="nav-item">     
              <button type="submit"  class="btn btn-sm-2 btn-dark">@ViewBag.Bn_names[2]</button>
            </li>
            <ul class="navbar-nav flex-grow-1"> 
          </form>  
                                
        </div>
       
        </nav>
        
